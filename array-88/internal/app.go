package internal

func Run() {

}

func Merge(nums1 []int, m int, nums2 []int, n int) {
	// Store it all in nums1
	// nums1 has m + n length
	// Sorted in non-decreasing order
	// Must run in O(m + n) time
	// The remainder of array nums1 is all 0
}
